{
  "name": "carioca-hub",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "dotenv_config_path=.env node -r dotenv/config dist/app.js",
    "dev": "tsnd -r dotenv/config --respawn --transpile-only src/app.ts dotenv_config_path=.env.dev",
    "build": "yarn tsc",
    "deploy": "git fetch --tags && git checkout `git rev-list --tags --max-count=1` && yarn && yarn prisma generate && yarn build && pm2 restart carioca-hub",
    "cli:create-env": "ts-node src/scripts/create-env.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/amqplib": "^0.10.7",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.6",
    "@types/node": "^20.11.25",
    "dotenv": "^16.5.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.2"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "amqplib": "^0.10.8",
    "axios": "^1.6.7",
    "bullmq": "^5.7.8",
    "cors": "^2.8.5",
    "cron": "^3.1.7",
    "express": "^4.18.3",
    "jsonwebtoken": "^9.0.2",
    "jwt-decode": "^4.0.0",
    "prisma": "^6.6.0",
    "zod": "^3.23.8"
  }
}
